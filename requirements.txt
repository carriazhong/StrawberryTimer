# Strawberry Timer - Python Dependencies

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0
pytest-asyncio>=0.21.0

# Note: No external sound library needed!
# The app uses built-in OS sound commands:
# - Windows: winsound (built-in)
# - macOS: afplay (built-in)
# - Linux: paplay/aplay (system tools)

# Note: tkinter is included with Python standard library
# On Ubuntu/Debian Linux: sudo apt-get install python3-tk
# On Windows/macOS: Already included with Python installer
