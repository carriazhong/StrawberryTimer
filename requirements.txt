# Strawberry Timer - Python Dependencies

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0
pytest-asyncio>=0.21.0

# Sound (cross-platform audio playback)
playsound>=1.3.0
pydub>=0.25.0  # Audio format conversion (optional, requires ffmpeg)

# Note: tkinter is included with Python standard library
# On Ubuntu/Debian Linux: sudo apt-get install python3-tk
# On Windows/macOS: Already included with Python installer
